"use client";
import{useEffect,useState}from"react";
const TABS=[['business','Business'],['verification','Approval & verification'],['branches','Branches'],['inventory','Inventory'],['commerce','Orders & quotes'],['delivery','Delivery'],['financial','Financial'],['reviews','Reviews'],['staff','Staff']] as const;
type TabId=(typeof TABS)[number][0];
export function SupplierDetailTabs({initialTab="business"}:{initialTab?:TabId}){const[active,setActive]=useState<TabId>(initialTab);useEffect(()=>{document.querySelectorAll<HTMLElement>('[data-supplier-tab]').forEach(panel=>{panel.hidden=panel.dataset.supplierTab!==active})},[active]);return <div className="card mt-4 flex gap-2 overflow-x-auto p-2" aria-label="Supplier detail sections" role="tablist">{TABS.map(([id,label])=><button key={id} id={`supplier-tab-${id}`} type="button" role="tab" aria-selected={active===id} aria-controls={`supplier-panel-${id}`} onClick={()=>setActive(id)} className={`whitespace-nowrap rounded-lg px-3 py-2 text-sm font-semibold ${active===id?'bg-brand-50 text-brand-700':'text-slate-600 hover:bg-slate-50'}`}>{label}</button>)}</div>}
