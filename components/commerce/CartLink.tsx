"use client";
import Link from"next/link";import{ShoppingCart}from"lucide-react";import{useEffect,useState}from"react";import{CART_UPDATED_EVENT,cartQuantity,readCart}from"@/lib/cart/cart";
export function CartLink(){const[count,setCount]=useState(0);useEffect(()=>{const update=()=>setCount(cartQuantity(readCart()));update();window.addEventListener(CART_UPDATED_EVENT,update);window.addEventListener("storage",update);return()=>{window.removeEventListener(CART_UPDATED_EVENT,update);window.removeEventListener("storage",update)}},[]);return <Link href="/cart" className="relative flex items-center gap-1 rounded-xl p-2" aria-label={`Cart with ${count} items`}><ShoppingCart/><span className="hidden text-sm font-semibold sm:inline">Cart</span>{count>0&&<span className="absolute -right-1 -top-1 min-w-5 rounded-full bg-brand-700 px-1 text-center text-xs font-bold text-white">{count}</span>}</Link>}
