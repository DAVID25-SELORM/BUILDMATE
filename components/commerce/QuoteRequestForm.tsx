"use client";
import { useState } from "react";
import { createQuoteRequest } from "@/app/(public)/request-quote/actions";
export function QuoteRequestForm() { const [error,setError]=useState<string|null>(null); const [pending,setPending]=useState(false); return <form className="card mt-8 space-y-5 p-7" action={async(formData)=>{setPending(true); const result=await createQuoteRequest(formData); setPending(false); if(result?.error)setError(result.error);}}><div><label className="label">Project name</label><input className="input" name="title" placeholder="e.g. East Legon 3-bedroom house" required/></div><div className="grid gap-5 md:grid-cols-2"><div><label className="label">Project location</label><input className="input" name="deliveryLocation" required/></div><div><label className="label">Required delivery date</label><input className="input" name="requiredDate" type="date"/></div></div><div><label className="label">Material list</label><textarea className="input min-h-40" name="materialList" placeholder="Enter one requirement per line" required/></div><div><label className="label">Additional instructions</label><textarea className="input min-h-28" name="notes"/></div>{error&&<p className="text-sm font-medium text-red-600" role="alert">{error}</p>}<button disabled={pending} className="btn-primary w-full">{pending?"Submitting...":"Submit quotation request"}</button></form> }
