"use client";import{useState}from"react";export function PayButton({orderId}:{orderId:string}){const[pending,setPending]=useState(false),[error,setError]=useState<string|null>(null);return <div><button className="btn-primary w-full" disabled={pending} onClick={async()=>{setPending(true);setError(null);const r=await fetch("/api/payments/hubtel/initiate",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({orderId})});const x=await r.json();if(!r.ok){setError(x.error??"Unable to start payment");setPending(false);return}window.location.assign(x.checkoutUrl)}}>{pending?"Opening secure checkout...":"Pay securely"}</button>{error&&<p className="mt-2 text-sm text-red-600">{error}</p>}</div>}
