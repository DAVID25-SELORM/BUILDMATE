"use client";import Link from"next/link";import{useState}from"react";import type{CartItem}from"@/lib/cart/cart";import{readCart,writeCart}from"@/lib/cart/cart";export function AddToCartButton({item}:{item:Omit<CartItem,"quantity">}){const[added,setAdded]=useState(false);function add(){const current=readCart();const found=current.find(x=>x.listingId===item.listingId);if(found)found.quantity+=1;else current.push({...item,quantity:1});writeCart(current);setAdded(true)}return <div><button className="btn-primary mt-5 w-full py-2.5" onClick={add} type="button">{added?"Added to cart":"Add to cart"}</button>{added&&<div className="mt-2 rounded-xl bg-emerald-50 p-3 text-sm text-emerald-900" role="status"><b>Added to cart</b><span className="block">{item.name}{item.variant?` — ${item.variant}`:""} · {item.supplier}</span><Link className="mt-1 inline-block font-bold underline" href="/cart">View cart</Link></div>}</div>}
