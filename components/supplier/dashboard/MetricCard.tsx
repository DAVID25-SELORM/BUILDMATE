type MetricCardProps = { label: string; value: string; helper?: string; actionLabel?: string };
export function MetricCard({ label, value, helper, actionLabel }: MetricCardProps) { return <div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-[0_1px_2px_rgba(15,23,42,0.04)]"><p className="text-xs font-semibold uppercase tracking-[0.08em] text-slate-500">{label}</p><p className="mt-3 text-2xl font-extrabold tracking-tight text-slate-950">{value}</p>{helper && <p className="mt-1 text-sm text-slate-500">{helper}</p>}{actionLabel && <div className="mt-4 text-sm font-semibold text-emerald-700">{actionLabel} →</div>}</div>; }
