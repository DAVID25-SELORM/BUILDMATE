"use client";import{useActionState}from"react";import{completeDelivery}from"@/app/driver/actions";export function CompleteDeliveryForm({id}:{id:string}){const[state,action,pending]=useActionState(completeDelivery,null);return <form action={action} className="mt-4 grid gap-2" encType="multipart/form-data"><input type="hidden" name="delivery" value={id}/><input className="input" name="otp" inputMode="numeric" maxLength={6} placeholder="Customer OTP" required/><input className="input" name="proof" type="file" accept="image/*" required/><button className="btn-primary" disabled={pending}>Complete delivery</button>{(state?.error||state?.message)&&<p className="text-sm">{state.error??state.message}</p>}</form>}
