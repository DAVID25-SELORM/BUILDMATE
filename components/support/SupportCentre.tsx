"use client";
import Link from "next/link";
import { HelpCircle, Search, X } from "lucide-react";
import { useEffect, useMemo, useRef, useState } from "react";

const topics=["How do I buy a product?","How do I compare suppliers?","What does ‘Confirm availability’ mean?","How do I request a quotation?","How do I track an order?","Can I cancel an order?","What happens with partial delivery?","How do refunds work?","Can I collect from the supplier?","How do I become a supplier?","How do suppliers update stock?"];
export function SupportCentre(){const[open,setOpen]=useState(false),[query,setQuery]=useState("");const close=useRef<HTMLButtonElement>(null);const matches=useMemo(()=>topics.filter((topic)=>topic.toLowerCase().includes(query.toLowerCase())),[query]);useEffect(()=>{if(open)close.current?.focus()},[open]);return <><button type="button" className="fixed bottom-5 right-4 z-[70] inline-flex items-center gap-2 rounded-full bg-brand-800 px-4 py-3 font-bold text-white shadow-xl hover:bg-brand-900 focus:outline-none focus:ring-2 focus:ring-brand-400 focus:ring-offset-2 sm:bottom-6 sm:right-6" onClick={()=>setOpen(true)} aria-haspopup="dialog"><HelpCircle className="h-5 w-5"/>Get Support</button>{open&&<div className="fixed inset-0 z-[80] bg-slate-950/45" onMouseDown={(event)=>{if(event.currentTarget===event.target)setOpen(false)}}><section role="dialog" aria-modal="true" aria-labelledby="support-title" className="absolute bottom-0 right-0 max-h-[90vh] w-full overflow-y-auto rounded-t-3xl bg-white p-6 shadow-2xl sm:bottom-6 sm:right-6 sm:w-[430px] sm:rounded-3xl"><div className="flex items-start justify-between"><div><h2 id="support-title" className="text-2xl font-black">How can we help?</h2><p className="mt-1 text-sm text-slate-600">Find guidance or send BuildMate a message.</p></div><button ref={close} type="button" className="rounded-lg p-2" aria-label="Close support centre" onClick={()=>setOpen(false)}><X/></button></div><label className="relative mt-5 block"><span className="sr-only">Search help</span><Search className="absolute left-3 top-3.5 h-4 w-4 text-slate-400"/><input className="input pl-10" placeholder="Search help" value={query} onChange={(event)=>setQuery(event.target.value)}/></label><div className="mt-5 flex flex-wrap gap-2">{["Shopping & products","Quotations","Orders","Payments","Delivery","Returns & refunds","Supplier support","Account & login","Technical problem"].map((item)=><span className="rounded-full bg-slate-100 px-3 py-1.5 text-xs font-semibold" key={item}>{item}</span>)}</div><div className="mt-6 space-y-2">{matches.map((topic)=><details className="rounded-xl border p-3" key={topic}><summary className="cursor-pointer font-semibold">{topic}</summary><p className="mt-2 text-sm text-slate-600">Visit the relevant product, quotation, order, or account page for the available action. If you still need help, send BuildMate the details below.</p></details>)}</div><Link href="/contact" className="btn-primary mt-6 block text-center" onClick={()=>setOpen(false)}>Contact BuildMate support</Link></section></div>}</>}
