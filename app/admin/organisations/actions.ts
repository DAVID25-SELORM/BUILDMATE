"use server";
import{cookies,headers}from"next/headers";import{redirect}from"next/navigation";import{createClient}from"@/lib/supabase/server";import{requirePermission}from"@/lib/auth/permissions";
export async function startCustomerOrganisationPreview(organisationId:string,formData:FormData){await requirePermission({permission:"customers.view"});const reason=String(formData.get("reason")??"").trim();if(reason.length<5)throw new Error("A preview reason is required");const h=await headers();const supabase=await createClient();const{data,error}=await supabase.rpc("start_admin_portal_preview",{target_portal:"customer",target_user:null,target_organisation:organisationId,target_reason:reason,target_reference:String(formData.get("referenceNumber")??"")||null,request_ip:h.get("x-forwarded-for")?.split(",")[0]?.trim()??null,request_user_agent:h.get("user-agent")??null});if(error||!data)throw new Error(error?.message??"Unable to start preview");const{error:contextError}=await supabase.rpc("configure_admin_portal_preview_context",{target_session:data,target_role_key:String(formData.get("previewRole")??"organisation_owner"),target_branch:null,target_warehouse:null,target_project:String(formData.get("previewProject")??"")||null});if(contextError)throw new Error(contextError.message);(await cookies()).set("admin_portal_preview",String(data),{httpOnly:true,sameSite:"lax",secure:process.env.NODE_ENV==="production",path:"/",maxAge:1800});redirect(`/admin/preview/customer-organisation/${organisationId}`)}
