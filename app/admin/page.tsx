import Link from"next/link";import{DashboardShell}from"@/components/dashboard/DashboardShell";import{MetricCard}from"@/components/dashboard/MetricCard";import{createClient}from"@/lib/supabase/server";
export default async function AdminDashboard(){const s=await createClient();const[{count:pending},{count:suppliers},{data:orders},{count:disputes}]=await Promise.all([s.from("organisations").select("id",{count:"exact",head:true}).eq("organisation_type","supplier").in("verification_status",["submitted","under_review"]),s.from("organisations").select("id",{count:"exact",head:true}).eq("organisation_type","supplier").eq("verification_status","approved"),s.from("orders").select("status,total"),s.from("order_disputes").select("id",{count:"exact",head:true}).in("status",["open","under_review"])]);const completed=(orders??[]).filter(o=>o.status==="completed");const sales=completed.reduce((sum,o)=>sum+Number(o.total),0);return <DashboardShell title="Platform administration" nav={[{label:"Overview",href:"/admin"},{label:"Suppliers",href:"/admin/suppliers"},{label:"Catalogue",href:"/admin/catalogue"},{label:"Settlements",href:"/admin/settlements"},{label:"Deliveries",href:"/admin/deliveries"},{label:"Disputes",href:"/admin/disputes"}]}><h1 className="text-3xl font-black">Platform overview</h1><p className="mt-2 text-slate-600">Live operational indicators.</p><div className="mt-6 grid gap-4 md:grid-cols-4"><MetricCard label="Completed sales" value={`GHS ${sales.toFixed(2)}`} detail="Recorded completed orders"/><MetricCard label="Completed orders" value={String(completed.length)} detail={`${orders?.length??0} total orders`}/><MetricCard label="Verified suppliers" value={String(suppliers??0)} detail={`${pending??0} awaiting review`}/><MetricCard label="Open disputes" value={String(disputes??0)} detail="Require administrator review"/></div><div className="mt-6 grid gap-6 lg:grid-cols-2"><div className="card p-6"><h2 className="text-xl font-bold">Supplier approvals</h2><p className="mt-3 text-sm text-slate-600">Review business and payout documents.</p><Link href="/admin/suppliers" className="btn-primary mt-5 inline-flex">Review {pending??0} applications</Link></div><div className="card p-6"><h2 className="text-xl font-bold">Payment reconciliation</h2><p className="mt-3 text-sm text-slate-600">Review provider totals and supplier ledger entries.</p><Link href="/admin/settlements" className="btn-secondary mt-5 inline-flex">Open reconciliation</Link></div></div></DashboardShell>}
