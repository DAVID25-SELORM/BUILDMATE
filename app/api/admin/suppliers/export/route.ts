import{requireRole}from"@/lib/auth/session";import{createClient}from"@/lib/supabase/server";const csv=(v:unknown)=>`"${String(Array.isArray(v)?v.join("; "):v??"").replaceAll('"','""')}"`;
export async function GET(request:Request){await requireRole(["admin","super_admin"]);const q=new URL(request.url).searchParams,s=await createClient(),rows:Record<string,unknown>[]=[];let page=1,total=1;while(rows.length<total&&page<=100){const{data,error}=await s.rpc("admin_list_suppliers",{search_text:q.get("q"),status_filter:q.get("status"),region_filter:q.get("region"),category_filter:q.get("category"),performance_filter:q.get("performance"),sort_by:q.get("sort")??"newest",page_number:page,page_size:100});if(error)return Response.json({error:error.message},{status:500});const batch=(data??[])as unknown as Record<string,unknown>[];rows.push(...batch);total=Number(batch[0]?.total_rows??0);page++}const headers=["business_name","trading_name","primary_contact","phone","email","business_type","region","categories","verification_status","verification_levels","product_count","order_count","total_sales","fulfilment_rate","average_rating","settlement_status","account_status","registered_at","performance_rating"];return new Response([headers.join(","),...rows.map(r=>headers.map(h=>csv(r[h])).join(","))].join("\r\n"),{headers:{"Content-Type":"text/csv; charset=utf-8","Content-Disposition":`attachment; filename="buildmate-suppliers-${new Date().toISOString().slice(0,10)}.csv"`,"Cache-Control":"no-store"}})}
