import{DashboardShell}from"@/components/dashboard/DashboardShell";import{SupplierOrdersView}from"@/components/dashboard/PortalSectionViews";import{requireSupplierPermission}from"@/lib/organisations/access";import{supplierNavigation}from"@/lib/organisations/navigation";import{confirmReturnToOrigin,recordCashPayment}from"./actions";
export default async function SupplierOrders(){const{supabase:s,membership}=await requireSupplierPermission("orders.view");const{data}=await s.from("orders").select("id,order_number,status,total,delivery_address,fulfilment_method,payment_method,supplier_received_at,created_at,deliveries(id,status)").eq("supplier_id",membership.organisationId).order("created_at",{ascending:false});const orders=data??[];const returns=orders.flatMap(order=>(order.deliveries??[]).filter(delivery=>delivery.status==="return_to_origin").map(delivery=>({delivery,order})));const awaitingCash=orders.filter(order=>order.status==="customer_confirmation_pending"&&(order.payment_method==="cash_on_delivery"||order.payment_method==="cash_on_pickup"));return <DashboardShell title="Supplier portal" nav={await supplierNavigation(membership.organisationId)}><SupplierOrdersView orders={orders}/>{awaitingCash.length>0&&<section className="card mt-6 p-5"><h2 className="text-xl font-black">Delivered orders awaiting cash confirmation</h2><p className="mt-2 text-sm text-slate-600">Record cash only after it is physically received. The customer must still confirm completion.</p><div className="mt-4 space-y-3">{awaitingCash.map(order=><div className="flex items-center justify-between gap-4" key={order.id}><div><b>{order.order_number}</b><p className="text-sm">GHS {Number(order.total).toFixed(2)} · {order.payment_method==="cash_on_pickup"?"Cash on Pickup":"Cash on Delivery"}</p></div><form action={recordCashPayment.bind(null,order.id)}><button className="btn-primary">Record cash received</button></form></div>)}</div></section>}{returns.length>0&&<section className="card mt-6 p-5"><h2 className="text-xl font-black">Returns to origin awaiting confirmation</h2><div className="mt-4 space-y-3">{returns.map(({delivery,order})=><form action={confirmReturnToOrigin} className="grid gap-2 sm:grid-cols-[1fr_2fr_auto]" key={delivery.id}><input type="hidden" name="delivery" value={delivery.id}/><b>{order.order_number}</b><input className="input" name="reason" minLength={5} placeholder="Confirm goods returned and give reason" required/><button className="btn-primary">Confirm return</button></form>)}</div></section>}</DashboardShell>}
