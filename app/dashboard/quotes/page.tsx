import{DashboardShell}from"@/components/dashboard/DashboardShell";import{CustomerQuotesView}from"@/components/dashboard/PortalSectionViews";import{createClient}from"@/lib/supabase/server";import{requireUser}from"@/lib/auth/session";import{getCustomerOrganisationMembership}from"@/lib/organisations/access";import{customerNavigation}from"@/lib/organisations/navigation";
export default async function CustomerQuotesPage(){const{user}=await requireUser();const s=await createClient();const{membership}=await getCustomerOrganisationMembership();const organisationId=membership?.organisation_id;let query=s.from("quote_requests").select("id,title,delivery_location,status,created_at,supplier_quotes(count)").order("created_at",{ascending:false});query=organisationId?query.eq("organisation_id",organisationId):query.eq("requester_id",user.id).is("organisation_id",null);const{data}=await query;return <DashboardShell title="Customer workspace" nav={await customerNavigation(organisationId)}><CustomerQuotesView requests={data??[]}/></DashboardShell>}
