import { QuoteRequestForm } from "@/components/commerce/QuoteRequestForm";
export default async function QuotePage({searchParams}:{searchParams:Promise<{title?:string;materials?:string;location?:string}>}){const q=await searchParams;return <section className="container-shell py-12"><div className="mx-auto max-w-3xl"><p className="font-semibold text-brand-700">PROCUREMENT REQUEST</p><h1 className="mt-2 text-4xl font-black">Request competitive quotations</h1><p className="mt-3 text-slate-600">Add your material requirements. Verified suppliers will respond privately.</p><QuoteRequestForm title={q.title?.slice(0,120)} materials={q.materials?.slice(0,4000)} location={q.location?.slice(0,200)}/></div></section> }
