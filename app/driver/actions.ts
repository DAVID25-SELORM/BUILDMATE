"use server";import{revalidatePath}from"next/cache";import{requireRole}from"@/lib/auth/session";import{createClient}from"@/lib/supabase/server";import{isDeliveryOtp}from"@/lib/delivery/otp";
export async function updateDelivery(formData:FormData){await requireRole(["driver"]);const{error}=await(await createClient()).rpc("driver_update_delivery",{target_delivery:String(formData.get("delivery")),new_status:String(formData.get("status"))});if(error)throw new Error(error.message);revalidatePath("/driver")}
export async function completeDelivery(_:unknown,formData:FormData){await requireRole(["driver"]);const id=String(formData.get("delivery")),otp=String(formData.get("otp")),file=formData.get("proof");if(!isDeliveryOtp(otp))return{error:"Enter the six-digit OTP"};if(!(file instanceof File)||file.size===0)return{error:"Proof photo is required"};if(file.size>5_000_000||!file.type.startsWith("image/"))return{error:"Upload an image under 5 MB"};const s=await createClient();const path=`${id}/${crypto.randomUUID()}-${file.name.replace(/[^a-zA-Z0-9._-]/g,"_")}`;const{error:uploadError}=await s.storage.from("delivery-proofs").upload(path,file,{contentType:file.type});if(uploadError)return{error:uploadError.message};const{error}=await s.rpc("driver_complete_delivery",{target_delivery:id,target_otp:otp,target_proofs:[path]});if(error){await s.storage.from("delivery-proofs").remove([path]);return{error:error.message}}revalidatePath("/driver");return{message:"Delivery completed"}}
