import{readFileSync,readdirSync}from"node:fs";import{join}from"node:path";import{describe,expect,it}from"vitest";
const dir=join(process.cwd(),'supabase','migrations'),sql=readdirSync(dir).filter(x=>x.endsWith('.sql')).map(x=>readFileSync(join(dir,x),'utf8')).join('\n').toLowerCase(),layout=readFileSync(join(process.cwd(),'app','admin','layout.tsx'),'utf8');
describe('admin security and audit guarantees',()=>{it('protects every admin route in the shared layout',()=>expect(layout).toContain('requireRole(["admin", "super_admin"])'));it('requires customer support permission for suspension and writes both histories',()=>{const body=sql.slice(sql.lastIndexOf('create or replace function public.admin_manage_customer'));expect(body).toContain("admin_has_permission('customer_support')");expect(sql).toContain("'customer_'||target_action");expect(sql).toContain('insert into public.audit_logs')});it('requires supplier verification permission for supplier suspension and approval',()=>{expect(sql).toContain("admin_has_permission('supplier_verification')");expect(sql).toContain('admin_set_supplier_status')});it('requires finance permission for settlement holds',()=>expect(sql).toContain("admin_has_permission('finance')"));it('keeps internal notes private behind RLS and revoked mutations',()=>{expect(sql).toContain('alter table public.admin_internal_notes enable row level security');expect(sql).toContain('revoke insert,update,delete on public.admin_internal_notes')});it('forces support sessions to read-only and records entry and exit',()=>{expect(sql).toContain('read_only boolean not null default true check(read_only)');expect(sql).toContain('read_only_preview_started');expect(sql).toContain('read_only_preview_ended')});it('prevents direct performance overwrites by authenticated users',()=>expect(sql).toContain('revoke insert,update,delete on public.admin_internal_notes,public.admin_action_history,public.support_view_sessions,public.supplier_performance_metrics from anon,authenticated'));it('limits permission changes to super administrators and audits them',()=>{expect(sql).toContain("role='super_admin'");expect(sql).toContain('admin_permission_granted');expect(sql).toContain("'permission_granted'")})});
