type Payload=Record<string,unknown>;export function renderEmail(template:string,p:Payload){switch(template){case"quote_received":return{subject:"New BuildMate quotation received",text:`A supplier responded to your request. Quote total: GHS ${p.total}. View it in your BuildMate dashboard.`};case"delivery_assigned":return{subject:"New BuildMate delivery assignment",text:`A delivery has been assigned to you for ${p.location}. Open your driver workspace for details.`};case"order_status_changed":return{subject:`BuildMate order ${p.order_number} updated`,text:`Your order status is now ${String(p.status).replaceAll("_"," ")}. Sign in to BuildMate to view the order.`};case"staff_invitation":return{subject:"You're invited to join BuildMate Ghana",text:`${p.fullName}, you have been invited to join BuildMate Ghana as ${p.roleLabel}. Accept your invitation: ${p.inviteUrl}. This link expires in 7 days. If you did not expect this invitation, you can ignore this email.`};case"password_reset":return{subject:"Reset your BuildMate password",text:`Use this secure link to choose a new BuildMate password: ${p.resetUrl}\n\nOpen the link promptly. It can only be used once. If you did not request a password reset, you can ignore this email.`};default:return{subject:"BuildMate update",text:"There is a new update in your BuildMate account."}}}
export function retryDelayMinutes(attempt:number){return Math.min(60,2**Math.max(0,attempt))}
